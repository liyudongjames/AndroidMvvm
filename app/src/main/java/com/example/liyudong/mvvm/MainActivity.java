package com.example.liyudong.mvvm;import androidx.appcompat.app.AppCompatActivity;import androidx.fragment.app.FragmentActivity;import androidx.lifecycle.Observer;import androidx.lifecycle.ViewModelProvider;import androidx.lifecycle.ViewModelProviders;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import android.os.Bundle;import com.example.liyudong.mvvm.net.bean.BaseBean;import com.example.liyudong.mvvm.net.bean.PublicArticalListBean;import com.example.liyudong.mvvm.viewmodel.ArticleListViewModel;import com.example.liyudong.mvvm.viewmodel.ViewModelFactory;import com.google.android.material.snackbar.Snackbar;import java.util.ArrayList;import java.util.List;public class MainActivity extends AppCompatActivity {    private ArticleListViewModel viewModel;    private RecyclerView ry_article;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        ry_article = findViewById(R.id.ry_article);        viewModel = obtainViewModel(this);        viewModel.getPublicArticalListLiveData().observe(this, new Observer<BaseBean<List<PublicArticalListBean>>>() {            @Override            public void onChanged(BaseBean<List<PublicArticalListBean>> publicArticalListBeanBaseBean) {            }        });        ry_article.setAdapter(new ArticleAdatper(new ArrayList<PublicArticalListBean>(), this));        ry_article.setLayoutManager(new LinearLayoutManager(this, RecyclerView.VERTICAL, false));        viewModel.getArtical();    }    public static ArticleListViewModel obtainViewModel(FragmentActivity activity) {        // Use a Factory to inject dependencies into the ViewModel        ViewModelFactory factory = ViewModelFactory.getInstance();        ArticleListViewModel viewModel =                ViewModelProviders.of(activity, factory).get(ArticleListViewModel.class);        return viewModel;    }}